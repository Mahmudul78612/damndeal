name: damnpay_sdk
description: DamnPay SDK — Add bill payments & recharge to any Flutter app
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  crypto: ^3.0.3
  webview_flutter: ^4.4.2
  url_launcher: ^6.3.1

flutter:
